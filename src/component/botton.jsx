import '../assets/css/btn.css'
const botton = (props) => {
    return (
        <div>
                    <button className="btn">{props.title}</button>
        </div>
    );
};
export default botton;